services:
  - type: web
    name: portfolio-api
    env: python
    plan: free

    buildCommand: "pip install --upgrade pip && pip install torch --index-url https://download.pytorch.org/whl/cpu && pip install --prefer-binary -r requirements.txt"

    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
